# MusicBeeQuickTag

[![Version](https://img.shields.io/badge/version-V2.1.4-blue.svg)](https://github.com/Stargazer-cc/MusicBeeQuickTag/releases)
[![MusicBee](https://img.shields.io/badge/MusicBee-3.0%2B-orange.svg)](https://getmusicbee.com/)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

一个 MusicBee 插件,用于快速浏览和应用音乐库中已存在的标签值。

---

## 功能特性

- **动态字段扫描**:自动扫描所有可用标签字段,无需手动配置
- **列式并排显示**:所有字段在一个界面中并排展示,一目了然
- **自定义显示顺序**:可以自定义界面中字段的显示顺序
- **实时选曲跟踪**:自动检测 MusicBee 中的选曲变化，可多选歌曲
- **高亮显示**: 选中歌曲已存在的标签值会高亮显示，多首歌时显示共有标签值
- **一键快速应用/删除标签值**:单击标签值即可应用到选中的歌曲，再次单击即可删除
- **预设组管理**: 将预设标签整理成不同的组，方便针对不同流派或场景快速应用

---

## 安装

1. 下载 `mb_MusicBeeQuickTag.dll` 文件
2. 打开首选项,选择"插件"选项卡,点击"添加插件"按钮,选中下载的 `mb_MusicBeeQuickTag.dll` 文件

---

## 🚀 使用指南
 
 ### 1. 配置字段
 在使用工具之前，请先选择需要管理的标签字段：
 1. 进入 `编辑` (Edit) → `首选项` (Preferences) → `插件` (Plugins)。
 2. 找到 **MusicBeeQuickTag** 并点击 `配置` (Configure)。
 3. 在设置窗口中：
    - **左侧列表**：显示从库中扫描到的所有可用字段。
    - **右侧列表**：显示当前已选定用于展示的字段。
    - 使用 `>>` 和 `<<` 按钮添加或移除字段。
    - 使用 `上移` (Move Up) 和 `下移` (Move Down) 调整显示顺序（设置中的从上到下对应工具界面中的从左到右）。
 4. 点击 `保存` (Save) 应用更改。
 
 ### 2. 浏览与应用标签
 1. **打开工具**：点击菜单栏的 `工具` (Tools) → `MusicBeeQuickTag`。窗口打开后会自动扫描库中现有的标签值。
 2. **选择歌曲**：在 MusicBee 主窗口中，选中你想要修改标签的歌曲文件。
    - 插件窗口顶部的 **状态栏** 会实时更新显示当前选中的文件数量（例如 “已选择 3 个文件”）或单首歌曲的详情。
 3. **应用标签**：
    - 在列出的字段中找到你想要应用的标签值。
    - **双击** 该值即可 **切换** 状态：
      - 如果歌曲没有该标签，则 **添加**。
      - 如果歌曲已有该标签（显示为蓝色/粗体），则 **移除**。
    - 插件会立即更新所有选中的歌曲。
    - 状态栏会显示确认信息。

 ### 3. 预设功能
 1. **配置**: 在设置窗口的"预设标签设置"标签页中，您可以创建新组并添加常用的标签值。
 2. **应用**: 在插件主窗口右上角选择一个预设组，然后点击 **应用** (Apply) 按钮，即可将该组内的所有标签一次性应用到选中的歌曲。

---

## 版本历史

**V2.1.4** (2025-12-29)
- 支持多组预设管理
- 已向工具栏配置中添加了启动插件的命令
- 增添了歌曲已添加标签的显示
- 在原有基础上优化了增添/删除标签值的交互方式

**v2.0** (2025-11-28)
- 动态扫描所有可用字段
- 支持自定义字段显示顺序
- 优化扫描性能(批量 API)
- 重构设置界面

**v1.x**
- 基础功能实现

---

## 许可证

[MIT License](LICENSE)
